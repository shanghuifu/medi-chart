.title
  %p 予約一覧 
  = link_to new_patient_path do
    患者登録
  = link_to new_reservation_path do
    予約登録
  = link_to patients_path do
    患者一覧
.body
  %table
    %tr
      %th 
        時間
      - for i in 0..13 do
        %th
          = (Date.today + i).strftime("%m/%d %a")
    - @schedules.each_with_index do |schedule, i|
      %tr
        %td
          = schedule.time
        - if @reservations[i].present?
          - 14.times do |days|
            %td
              - @reservations[i].each do |rsv|
                = rsv.rsv_date_check(days)